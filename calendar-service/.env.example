# Server Configuration
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/calendar_db

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Security (optional - if not set, API auth is disabled for development)
API_KEY=your-api-key-here

# Rate Limiting (optional - defaults to 100 requests/minute)
RATE_LIMIT_MAX=100

DB_MAX_CONNECTIONS=

DB_IDLE_TIMEOUT

DB_CONNECT_TIMEOUT